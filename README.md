# JOE Website V2

Jawa Offworld Enterprises website and member tools.

## Current Version

- Production release: `v2.0.0`
- Active development branch: `v2.0.1`

## Branch Status

- `dev` is the working branch for `v2.0.1` updates
- production should only receive pulled, tested changes from `dev`

## Notes

- `v2.0.1` is intended for post-release fixes, polish, and small improvements
- major new work should be planned deliberately instead of being folded into patch updates

## v2.0.1 Status

- `v2.0.1` is the next planned patch release
- changes are being prepared on `dev`
- current `dev` updates include Astrogation legend toggles and legend usability improvements
- release notes should be added here once `v2.0.1` work is finalized
